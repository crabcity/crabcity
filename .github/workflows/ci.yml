name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gates:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Restore Bazel repository cache
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/bazel-repo-cache
          key: bazel-repo-${{ hashFiles('MODULE.bazel') }}
          restore-keys: |
            bazel-repo-

      - name: Restore Bazel disk cache
        uses: actions/cache/restore@v4
        with:
          path: ~/.cache/bazel-disk-cache
          key: bazel-disk-${{ hashFiles('MODULE.bazel', 'Cargo.lock', 'packages/crab_city_ui/pnpm-lock.yaml') }}
          restore-keys: |
            bazel-disk-

      - name: Run quality gates
        run: scripts/quality_gates.sh --verbose
        env:
          BAZEL_EXTRA_ARGS: "--disk_cache=${HOME}/.cache/bazel-disk-cache --repository_cache=${HOME}/.cache/bazel-repo-cache"

      - name: Save Bazel repository cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/bazel-repo-cache
          key: bazel-repo-${{ hashFiles('MODULE.bazel') }}

      - name: Save Bazel disk cache
        if: always()
        uses: actions/cache/save@v4
        with:
          path: ~/.cache/bazel-disk-cache
          key: bazel-disk-${{ hashFiles('MODULE.bazel', 'Cargo.lock', 'packages/crab_city_ui/pnpm-lock.yaml') }}
